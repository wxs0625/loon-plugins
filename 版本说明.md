# 集合修复版版本说明

## 使用建议

- 当前推荐版本：`集合修复版_3.10.sgmodule`
- `集合修复版_3.8.sgmodule` 会导致闪动校园 100% 闪退，请立即停用并删除。
- `集合修复版_3.9.sgmodule` 仍有闪动校园闪退问题（保留的广告重写返回空对象导致 SDK 崩溃），请升级到 3.10。
- 所有中文版本都是历史文件的独立副本，没有覆盖或删除原文件。
- 文件名只使用产品名和版本号，具体功能记录在本文档中。
- `独立模块_1.0.sgmodule` 不属于集合版本线，不要与集合版同时启用相同的微博规则。

## 集合修复版 1.0

文件：`集合修复版_1.0.sgmodule`

- 将 Loon 集合修复版转换为 Shadowrocket 模块格式。
- 合并本地 33 个 Shadowrocket 模块。
- 包含广告拦截、应用净化、URL 重写、响应体处理、Map Local 和 MITM 配置。
- 初始版本中的 YouTube 响应处理可能导致联网变慢。

## 集合修复版 2.0

文件：`集合修复版_2.0.sgmodule`

- 基于 1.0。
- 加强纵横小说启动接口拦截。
- 删除 YouTube 响应脚本、Google Video `initplayback` 处理和过宽的 Google API 重写。
- 移除相关高开销 MITM，降低 YouTube 联网延迟。
- 原来的两个 v2 英文文件内容完全相同，中文版本只保留一份 2.0。

## 集合修复版 3.0

文件：`集合修复版_3.0.sgmodule`

- 基于 2.0。
- 补入之前因扩展名不同而遗漏的微博模块。
- 增加微博启动页、信息流、评论、话题、搜索、皮肤和图标等广告及净化处理。
- 保留纵横小说修复和 YouTube 低延迟调整。

## 集合修复版 3.1

文件：`集合修复版_3.1.sgmodule`

- 基于 3.0。
- 调整微博 MITM 主机顺序，提高微博相关主机的匹配优先级。
- 规则功能与 3.0 基本一致，主要变化是微博解析顺序。

## 集合修复版 3.2

文件：`集合修复版_3.2.sgmodule`

- 基于 3.0 的完整功能版本。
- 修复两个错误拼接的 MITM 主机。
- 删除已被同级通配符覆盖的精确 MITM 主机。
- 不删除广告脚本和响应体处理，在不影响完整广告拦截的前提下降低匹配开销。
- 这是保守省电版本，也是后续均衡版本的可靠基线。

## 集合修复版 3.3

文件：`集合修复版_3.3.sgmodule`

- 基于 3.2。
- 停用部分被判断为非广告的增强脚本和响应处理，以进一步降低耗电。
- 后续确认其中部分脚本同时承担广告和页面布局处理，导致部分广告拦截失效。
- **不推荐使用，仅作为历史版本保留。**

## 集合修复版 3.4

文件：`集合修复版_3.4.sgmodule`

- 修复 3.3 的广告漏拦问题。
- 恢复被误删的混合广告及布局处理。
- 只停用严格确认与广告无关的 7 条脚本和 1 条响应体处理。
- 在广告拦截完整性和耗电之间采用更保守的平衡方案。

## 集合修复版 3.5

文件：`集合修复版_3.5.sgmodule`

- 基于 3.4。
- 修复微信公众号 `getappmsgad` 同时被直接拒绝和响应体重写处理的冲突。
- 从组合拒绝规则中移除 `getappmsgad`，让微信公众号广告响应能够进入净化流程。
- 仍使用字段文本替换，部分微信版本可能出现广告素材消失但容器留白。

## 集合修复版 3.6

文件：`集合修复版_3.6.sgmodule`

- 基于 3.5，是当前推荐版本。
- 移除微信公众号旧的 `advertisement` 字段文本替换。
- 新增结构化 JSON 净化脚本，将 `advertisement_num` 设为 `0` 并清空 `advertisement_info`。
- 减少公众号正文和文章底部广告被拦截后留下空白占位的情况。
- 净化脚本保存在自己的 GitHub 仓库，避免第三方脚本变化。
- 保留微博、小红书、纵横小说及其他广告处理。
- 不恢复 YouTube 响应脚本和 Google Video 高延迟 MITM。
- 微信客户端自身固定的原生布局可能仍会保留空白区域，代理模块无法直接修改客户端界面。

## 集合修复版 3.7

文件：`集合修复版_3.7.sgmodule`

- 基于 3.6。
- 修复闪动校园启动时可能因快手广告 SDK 初始化失败而闪退的问题。
- 移除 `open.e.kuaishou.com` 和 `e.kuaishou.com` 的冲突拒绝规则。
- 移除两个 `open/e/v3/open/univ` 硬断连重写及同接口的响应脚本。
- 为闪动校园认证、广告 SDK 初始化及广告响应相关域名添加优先直连规则。
- 保留其他广告拦截处理；这部分快手广告请求改为由应用正常处理，避免 SDK 因连接中断导致异常。
- 后续确认仍遗漏宽泛的快手和 1RTB 空对象重写，可能继续造成间歇闪退，因此不再推荐。

## 集合修复版 3.8

文件：`集合修复版_3.8.sgmodule`

- 基于 3.7。
- **已确认缺陷：新增 `api.huachenjie.com` 的 MITM 会导致闪动校园启动 100% 闪退，不推荐使用，已由 3.9 替代。**
- 删除 3.7 遗漏的宽泛快手 `open` 重写，以及三条 1RTB `req_ad` 空对象重写。
- 快手与 1RTB 广告 SDK 初始化请求改为正常响应，避免 SDK 收到不合法空对象后间歇异常。
- 新增闪动校园自有的顶部弹窗、首页广告和“我”页面福袋三个明确接口处理。
- 只为 `api.huachenjie.com` 补充 MITM，不恢复快手或 1RTB 的 MITM 和响应脚本。
- 保留微信公众号、微博、小红书、纵横小说等既有处理，不恢复 YouTube 响应脚本和 Google Video 高延迟 MITM。
- 首次测试前建议清除闪动校园缓存，避免旧开屏素材继续显示。
- 若清除缓存后仍有开屏广告，应根据 Shadowrocket 请求日志和真实响应编写结构化净化脚本，不应恢复硬断连或空对象响应。

## 集合修复版 3.9

文件：`集合修复版_3.9.sgmodule`

- **基于 3.6 重构**（放弃 3.7/3.8 路线），是当前推荐版本。
- 3.8 把 `api.huachenjie.com`（闪动校园自家接口）加入 MITM，破坏证书校验，导致 App 启动 100% 闪退。3.9 完全不动 MITM。
- 闪动校园的开屏广告由 App 内置的快手广告 SDK 提供，所以之前一直在修快手；但任何对快手/1RTB 请求的拦截都会让 SDK 初始化失败而闪退。
- 删除 3.6 里全部快手相关处理：3 条快手域名 REJECT 规则、2 条快手下载器域名 REJECT 规则、6 条快手/1RTB 空对象重写、1 条含快手接口的广告联盟脚本（该脚本在 3.6 中本就不生效，因为 MITM 没有快手主机）。
- 新增 5 条直连规则：`huachenjie.com`、`open.e.kuaishou.cn`、`open.e.kuaishou.com`、`ad.shunchangzhixing.com`、`1rtb.net`，保证 App 主接口和广告 SDK 初始化请求直连放行。
- 新增 3 条闪动校园自有广告接口拦截（顶部弹窗、首页广告、福袋）。注意：两个 `https` 接口在未启用 `api.huachenjie.com` MITM 时不会触发（这是为了稳定故意放弃），`http` 的福袋接口拦截正常生效。
- 保留微信公众号、微博、小红书、纵横小说等全部既有处理，微信净化脚本不变。
- 启用前请停用并删除 3.7/3.8 及任何闪动校园专用模块，清除闪动校园缓存后再测试。
- **已确认缺陷：`reject-dict` 返回空对象同样会让广告 SDK 崩溃，闪动校园仍 100% 闪退，请升级到 3.10。**

## 集合修复版 3.10

文件：`集合修复版_3.10.sgmodule`

- **基于 3.9 修复**，是当前推荐版本。
- 删除 3.9 新增的 3 条闪动校园广告重写（`getPopup`、`run-front/ad`、`getAd`）。原因：给广告 SDK 返回空对象（`reject-dict`）和给快手/1RTB 返回空对象一样，都会让 SDK 崩溃，导致 100% 闪退。
- 保留 3.9 的 5 条 DIRECT 直连规则和全部快手/1RTB 拦截删除——闪动校园的快手广告 SDK 初始化请求完全放行，不再闪退。
- 保留 3.9 的 MITM 不动（不新增任何主机），不碰闪动校园自家接口的证书链路。
- 新增 3 条京东开屏广告拦截：`queryMaterialAdverts` 物料广告 API、`bdsp-x/dsp-x.jd.com/adx/sdk/open` DSP 开屏 SDK、`360buyimg.com/jddjadvertise/` 开屏广告图片 CDN。
- 新增 3 条百度网盘开屏广告拦截：`issuecdn.baidupcs.com/issue/netdisk/guanggao/` 广告素材（之前只覆盖 `ts_ad`，现在补上 `guanggao`）、`staticsns.cdn.bcebos.com/amis/.../banner.png` 开屏横幅、`d3g6o6c6r6f4.cdn.bcebos.com/...mp4` 开屏视频。
- 保留微信公众号、微博、小红书、纵横小说等全部既有处理，微信净化脚本不变。
- 启用前请停用并删除 3.7/3.8/3.9，清除闪动校园、京东、百度网盘缓存后再测试。

## 独立模块 1.0

文件：`独立模块_1.0.sgmodule`

- 独立微博广告拦截模块。
- 包含微博启动页、信息流、评论、话题、搜索、皮肤和图标等处理。
- 适合不使用完整集合版、只需要微博处理的情况。
- 完整集合版 3.0 及之后版本已经包含对应微博能力，不应重复启用。