# 集合修复版 3.9

文件：`集合修复版_3.9.sgmodule`
构建脚本：`build_collection_3_9_shandong_campus_from_3_6.ps1`

## 为什么从 3.6 重新开始

- 3.7 移除了部分快手拦截，但遗漏了宽泛的 1RTB 空对象重写，闪动校园仍间歇闪退。
- 3.8 补删了遗漏的重写，但把 `api.huachenjie.com`（闪动校园自家主接口）加入 MITM，破坏证书校验，导致 App 启动 100% 闪退。
- 结论：3.7/3.8 的路线不可信，3.9 放弃该路线，从最后稳定的 3.6 重构。

## 闪动校园闪退和快手的关系

闪动校园的开屏广告不是 App 自己实现的，而是内置了快手广告 SDK（open.e.kuaishou.com / sdk.1rtb.net / zlsdk.1rtb.net）来拉取和展示。任何对该 SDK 初始化请求的拦截（域名拒绝、空对象响应、硬断连）都会让 SDK 抛异常，直接带崩整个 App。所以修复闪退的关键不是"拦截快手广告"，而是"完全不要碰快手请求"。

## 3.9 相对 3.6 的改动

删除（全部为快手/1RTB 相关）：

- 3 条域名规则：`DOMAIN,open.e.kuaishou.com,REJECT`、`DOMAIN-SUFFIX, e.kuaishou.com, REJECT`、`DOMAIN-SUFFIX,e.kuaishou.com, REJECT`
- 2 条下载器规则：`cm.downloader.commercial.kuaishou.com` 两种写法
- 6 条重写：`open.e.kuaishou.cn/rest/e/v3/open` 空对象、`sdk.1rtb.net/sdk/req_ad?` 空对象、`zlsdk.1rtb.net` 带参空对象、`zlsdk.1rtb.net` 宽泛空对象、`open/e/v3/open/univ` 两个硬断连
- 1 条脚本：快手联盟/优量汇/穿山甲 `adsense.js`（3.6 的 MITM 里本就没有快手主机，该脚本从未对快手请求生效，删除无影响）

新增：

- 5 条直连规则（置于 `[Rule]` 段首）：`huachenjie.com`、`open.e.kuaishou.cn`、`open.e.kuaishou.com`、`ad.shunchangzhixing.com`、`1rtb.net`
- 3 条自有广告接口重写：`getPopup` 顶部弹窗、`run-front/ad` 首页广告、`getAd` 福袋（前两个 https 接口在未启用对应 MITM 时不会触发，这是为了稳定故意放弃的；http 的福袋接口正常生效）

不变：

- MITM 列表完全不动，不新增任何主机
- 微信公众号结构化净化、微博、小红书、纵横小说等全部保留

## 使用要求

1. 停用并删除 3.7、3.8 以及任何闪动校园专用模块，只启用 3.9。
2. 清除闪动校园缓存（或卸载重装）后再测试。
3. 如果仍闪退，说明原因不在模块，请检查是否同时启用了其他含快手/1RTB 规则的模块或全局规则。

## 校验

- 模块 SHA-256：`256EFC8DB37E66CD0FFA775422825FAF7E3FC50FA21AD1913C42EF7F611A7B06`
- 源文件 3.6 SHA-256（构建后未变）：`53AD6E37DFF97F5C904C9F1A325F3891D829169584EF62838C77C69351A1960F`
- 构建断言：5 条 DIRECT 规则、5 条域名规则删除、6 条重写删除、1 条脚本删除、3 条广告重写，全部通过。
